<% text = render :partial => 'reports/hours' %>
<% text.concat(render :partial => 'reports/goals') %>
<% text.concat(render :partial => "reports/#{@report.type}") %>

<% markdown = BlueCloth.new(text) %>

<%= raw markdown.to_html %>
